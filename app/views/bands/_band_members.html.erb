<li>
  <%= gravatar_for member.user, size: 50 %>
  <%= link_to member.user.name, member.user %>
  <% if is_band_admin?(@band.id) && !current_user?(member.user) && !member.is_admin? %>
    | Remove from Band
  <% end %>
</li>
